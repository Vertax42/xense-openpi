# Video-playback laptop deps (Windows / macOS). Intentionally minimal — no openpi,
# no xense_client. msgpack_numpy.py is vendored alongside this file.
websockets>=12.0
msgpack>=1.0.0
numpy>=1.23.0
# Needed only by the shoe_sm detector (OpenCV blue-insole detection); the gripper
# and stub detectors do not import cv2.
opencv-python>=4.5.0
